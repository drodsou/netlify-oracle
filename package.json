{
  "name": "netlify-oracle",
  "version": "0.1.0",
  "description": "Netlify function to query Oracle database",
  "main": "public/index.html",
  "scripts": {
    "build": "scripts/decrypt-networkadmin.sh",
    "dev": "scripts/dev.sh",
    "precommit": "scripts/encrypt-networkadmin.sh",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "@drodsou",
  "license": "ISC",
  "dependencies": {
    "oracledb": "^5.1.0"
  },
  "devDependencies": {
    "netlify-cli": "^3.13.7",
    "netlify-plugin-encrypted-files": "0.0.5"
  }
}
